#!/usr/bin/env zsh
# 将软件添加到插件的 Brewfile 中
#
# 实现逻辑:
# - name: 将第一个参数作为插件名称，查询 plugin_home 目录是否存在，不存在报错直接返回
# - 其余参数都传给 brew bundle add 命令

name=$1
plugin_home=$(wzsh plugin home ${name})
brewfile=${plugin_home}/Brewfile

brew bundle --file=${brewfile} add ${@:2}
