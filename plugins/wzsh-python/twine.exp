#! /usr/bin/env expect
set username "$env(TWINE_USERNAME)"
set password "$env(TWINE_PASSWORD)"

spawn twine upload dist/* --verbose

expect "username:"

send "$username\n"

expect "password:"

send "$password\n"

interact
